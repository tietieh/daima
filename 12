#include<stdio.h>
#include<math.h> 
int main(){
	printf ("请输入三个数");
	double a,b,c;
	scanf("%lf%lf%lf",&a,&b,&c);
	double p;p=(a+b+c)*0.5;
	double f;f=sqrt(p*(p-a)*(p-b)*(p-c));
	if((a+b>c)&&(a+c>b)&&(b+c>a)) printf("其面积为%f",f);
	else printf("不能组成三角形");
}
